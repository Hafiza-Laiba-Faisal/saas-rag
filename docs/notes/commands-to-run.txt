# Run (Python backend on :3001, serves the SPA from chic-interface-design/dist-spa)

cd /home/tenbitsolutions/Documents/Tenbit/3-ongoing-projects/RAG
set -a; . ./.env; set +a
PYTHONPATH=src .venv/bin/uvicorn rbs_rag.web.server:app --host 0.0.0.0 --port 3001 --reload

# Frontend dev server (optional, proxies /api -> http://127.0.0.1:3001)
cd chic-interface-design && npm run dev

# Build the SPA (Node >= 22)
cd chic-interface-design && npm run build
